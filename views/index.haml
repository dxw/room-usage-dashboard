%h1 Room Usage

%h2 Ground Floor Meeting Room
- if @ground_floor_events
  %ul
    - @ground_floor_events.each do |event|
      %li
        %strong
          = event.summary
        = "(#{event.start.date || event.start.date_time})"

%h2 First Floor Meeting Room (The Hide)
- if @the_hide_events
  %ul
    - @the_hide_events.each do |event|
      %li
        %strong
          = event.summary
        = "(#{event.start.date || event.start.date_time})"

%h2 Wellbeing Room
- if @wellbeing_room_events
  %ul
    - @wellbeing_room_events.each do |event|
      %li
        %strong
          = event.summary
        = "(#{event.start.date || event.start.date_time})"
